<?xml version="1.0" encoding="utf-8"?>
<!--
   Excludes locally-cached auth/session data and preferences from Android backup
   and device-to-device transfer. The source of truth is the Supabase backend, so
   nothing of value is lost and a cached refresh token cannot leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
